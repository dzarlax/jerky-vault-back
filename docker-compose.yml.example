version: '3.8'

services:
  jerky_vault_backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      DB_USER: 
      DB_PASSWORD: 
      DB_HOST: 
      DB_PORT: 
      DB_NAME: 
      JWT_SECRET: 
      FRONT_URL: 

